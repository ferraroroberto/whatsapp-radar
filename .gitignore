# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / TypeScript
node_modules/
dist/
coverage/
*.tsbuildinfo

# Virtual envs
.venv/
venv/

# Local config and secrets
.env
.env.*
!.env.example
config/local.*
config/*.local.*

# Webapp runtime + secrets (added by Step 3 — committed only as *.sample templates)
webapp/*.log
webapp/last_tunnel_url.txt
# Execution-tab run records: run.json + output.log carry chat names/message text.
webapp/runs/
# Real Tailscale-issued HTTPS cert (scripts/gen_tailscale_cert.py) — machine-specific.
webapp/certificates/
config/webapp_config.json
config/webauthn_devices.json
config/cloudflared.yml

# WhatsApp linked-device credentials and local private data
auth/
sessions/
data/
*.sqlite
*.sqlite3
*.db
*.db-shm
*.db-wal
*.log
qr*.png
qr*.txt

# App Launcher scratch
.launcher-tmp/

# Agent / IDE / OS
.claude/
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Private, uncommitted local context/spec files (household spec for #160, etc.)
_local/
