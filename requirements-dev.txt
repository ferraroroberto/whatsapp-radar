# Test / lint / type-check dependencies. Install with:
#   .venv\Scripts\python.exe -m pip install -r requirements-dev.txt

pytest>=8.0
ruff>=0.6
mypy>=1.10

# Webapp tests: in-process FastAPI TestClient + async routes.
pytest-asyncio>=0.23

# End-to-end browser smoke tests (tests/e2e). After install:
#   .venv\Scripts\python.exe -m playwright install chromium webkit
# Chromium catches markup/JS regressions; WebKit projects the suite onto an
# iPhone viewport so iOS-engine bugs get caught on Windows.
pytest-playwright>=0.5

# Bounded retry for the known-flaky WebKit/iPhone e2e leg (issue #64): a slow
# hosted runner occasionally wedges that projection, so tests/e2e/conftest.py
# gives only the WebKit items a couple of reruns. Chromium stays loud.
pytest-rerunfailures>=14
